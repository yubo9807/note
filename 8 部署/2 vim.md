# vim

- `vim file.txt` 打开文件
- `v` 选择模式
- `V` 行选择模式
- `control` + `v` 块选择模式
- `y` 复制
- `p` 粘贴
- `d` 删除行
- `x` 向后删除
- `i` 插入
- `I` 首行插入
- `r` 覆盖文字
- `u` 撤销
- `j` 下一行
- `w` 下一个单词
- `g` 回到顶部
- `esc` 退出编辑
- `:q` 退出文件
- `:wq` 保存退出
- `:q!` 不保存强制退出
- `:set nu` 显示行号
